import { ButtonP } from './button.options';
export default function Button({ buttonTheme, className, onPress, title, children, titleProps, disabled, showPreloader, throttleTime, titleClassName, forceShowPreloader, ...p }: ButtonP): import("react/jsx-runtime").JSX.Element;
