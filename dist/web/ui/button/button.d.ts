import { ButtonP } from './button.options';
export declare const Button: ({ onClick, children, throttleTime, showPreloader, title, colorPreloader, titleClassName, className, titleProps, disabled, ...p }: ButtonP) => import("react/jsx-runtime").JSX.Element;
